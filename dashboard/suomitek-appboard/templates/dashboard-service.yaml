apiVersion: v1
kind: Service
metadata:
  name: {{ template "suomitek-appborad.dashboard.fullname" . }}
  labels:
    app: {{ template "suomitek-appborad.name" . }}
    chart: {{ template "suomitek-appborad.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.dashboard.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "suomitek-appborad.dashboard.fullname" . }}
    release: {{ .Release.Name }}
